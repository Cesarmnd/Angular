import { StudentStateDirective } from './student-state.directive';

describe('StudentStateDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentStateDirective();
    expect(directive).toBeTruthy();
  });
});
